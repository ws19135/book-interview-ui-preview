# 新書訪談邀約系統 · 同事測試說明

**要花的時間：15–20 分鐘。**
你不需要任何技術背景，也不用登入任何帳號。

---

## 這一輪在問什麼

> 這個系統的**畫面與流程**，跟你實際的工作方式對不對得上。

**不是**在問：權限對不對、資料正確不正確、安全不安全、能不能上線。
那些是後面幾輪的事，這一輪先確定「路走對了」。

---

## 開始之前，三件事先知道

1. **這是示意稿，不是真的系統。** 所有名字、書名、節目、Email 都是編的。
2. **你按什麼都不會壞。** 沒有連任何資料庫，重新整理頁面就全部回到原樣。
3. **你的回饋不會自動送出。** 填完要自己按「匯出」，把檔案傳回來。

---

## 第一步 · 打開預覽（1 分鐘）

用電腦瀏覽器打開專案負責人給你的網址。

打開後最上面會有一條紅色橫幅寫著「互動示意稿｜全部為假資料｜不連資料庫」——看到它就對了。

---

## 第二步 · 先看完整流程（4 分鐘）

點上方的 **「模式 B · 完整流程藍圖」**。

由上往下看一遍 15 個階段。這是系統預定的完整生命週期：從需求進來，到節目上架、素材收齊、結案。

每個階段下面有標籤，告訴你這一段「驗到什麼程度」：

| 標籤 | 意思 |
|---|---|
| 已實作且 live 驗證 | 做好了，而且在真的系統上跑過 |
| 已實作但僅 R2 驗證 | 做好了，但只有一種身分試過 |
| 已由本機 harness 驗證 | 做好了，但只在測試環境驗過 |
| 尚未多角色驗證 | 還沒有不同職務的人各自試過 |
| Phase 2／後續規劃 | 之後才做 |
| 不在目前範圍 | 這次不做 |

看完最下面那一區「不在這一輪範圍的事」——如果你想找的功能在那裡，那是**預期的**，不是漏掉。

---

## 第三步 · 照著情境操作（8 分鐘）

點回 **「模式 A · 目前已完成」**，照下面順序走一遍。

### 情境一：一筆新需求從進來到成案

1. 在 **工作總覽** 看三個數字，按「前往需求清單」。
2. 按 **「建立需求」**，隨便填一個假名字和假 Email（用 `xxx@example.invalid`），送出。
   - 👀 看清單有沒有多一筆。
3. 按 **「再送出一次同一筆（測冪等）」**，填一模一樣的姓名和聯絡方式，送出。
   - 👀 系統應該說「這筆已經處理過了」，而且清單**不會多一筆**。
4. 找到 `REQ-DEMO-001`，按 **「開啟」**。
5. 按 **「審核」** → 選「接受」 → 填原因 → 送出。
6. 按 **「加入候選節目」**，填一個假節目編號，送出。
7. 在下面候選節目那一列，依序按 **確認歸屬 → 提案 → 登錄回覆（選接受）**。
8. 按 **「建立訪談案件」**，送出。
9. 開啟那筆訪談案件，按 **「更新製作進度」**。
   - 👀 下拉裡**只有下一步**可以選，不能跳步。
10. 按 **「素材完整性」**，更新一筆素材的「交付」與「收訖」。
    - 👀 這兩個是分開的：對方寄了 ≠ 我們收到了。

### 情境二：個資是怎麼保護的

11. 回 **需求清單**，看「需求方聯絡」那一欄——預設是 `***`。
12. 按其中一個 **「查看」**。
13. 去 **稽核查閱**。
    - 👀 你剛剛那次「查看」被記下來了。

### 情境三：兩個人同時改同一筆

14. 在任何一頁最下面找到 **「Demo 工具」**，按 **「模擬版本衝突（別人先改了）」**。
15. 回需求清單開一筆需求，做任何一個「審核／更新／取消」。
    - 👀 系統會擋下來，要你重新整理。這是防止你蓋掉同事的修改。

### 情境四：高風險操作

16. 開一筆需求，按 **「取消需求」**。
    - 👀 一定要填原因、一定要勾確認，少一個就送不出去。

### 情境五：找不到的東西

17. 在頁面下方按 **「模擬猜 ID 越權」**，三個都試一次。
    - 👀 不管是不存在的編號還是亂打的字，都是同一頁同一句話。

### 情境六：需求方那一端看到什麼

18. 按 **「公開收件表單」**。
    - 👀 預設是「暫停收件」——這就是**現在真實系統的狀態**。
19. 按 **「▢ 目前展示：暫停收件」** 切成開放，看看那 17 題。
20. 把第 1 題在「有書找節目 / 有節目找來賓」之間切換。
    - 👀 第 5、6 題會跟著換。
21. 隨便按「送出需求」不填東西。
    - 👀 系統會告訴你還有幾題沒填。

---

## 第四步 · 回答這些問題（5 分鐘）

捲到頁面最下面的 **「測試回饋」**。每看完一個畫面填一則，特別針對這幾件事：

1. **你是否知道下一步該按什麼？**
2. **頁面上的狀態名稱是否符合工作用語？**（例：「名單研擬」你們平常怎麼說？）
3. **哪些資訊太多或太少？**
4. **哪個流程順序與實際工作不同？**
5. **哪些動作不應該由你的職務執行？**
6. **哪些高風險動作需要主管確認？**
7. **手機操作是否看得清楚？**（用手機再打開一次看看）
8. **有沒有一個工作情境，在系統裡完全找不到入口？** ← 這題最重要

嚴重度怎麼選：

| 選項 | 什麼時候用 |
|---|---|
| **阻擋** | 這樣做我沒辦法用這個系統工作 |
| **重要** | 可以用，但會一直出錯或很花時間 |
| **建議** | 用得下去，但有更好的做法 |

---

## 第五步 · 傳回來（1 分鐘）

1. 按 **「匯出 Markdown」**。
2. 把出現的整段文字**全選複製**。
3. 貼進 Email 或訊息傳回專案負責人。

（也可以按「複製回饋摘要」直接複製到剪貼簿。）

⚠️ **頁面不會自動上傳任何東西。** 沒有匯出等於沒有回饋。

---

## 常見狀況

| 你遇到 | 怎麼辦 |
|---|---|
| 按一按不知道走到哪了 | 按上方 **「重置 Demo」**，回到最初狀態（回饋不會被清掉） |
| 想再看一次某個畫面 | 頂端四個入口點回去；往下鑽的頁面用麵包屑回上一層 |
| 手機上字太小 | 直立拿、不要橫的；表格可以左右滑 |
| 回饋不見了 | 回饋存在你自己的瀏覽器裡。換電腦、換瀏覽器、無痕視窗都看不到 |

---

## 最後再說一次

- 這一輪是 **UI／流程驗收**。
- **不是**權限驗收、**不是**安全驗收、**不是**資料正確性驗收、**不是**正式上線驗收。
- 你在這裡按的任何東西，都不會影響任何真實資料。
