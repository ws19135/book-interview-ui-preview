# 新書訪談邀約｜同事指南

👉 **打開最新版驗收頁：[https://ws19135.github.io/book-interview-ui-preview/](https://ws19135.github.io/book-interview-ui-preview/)**

約 30 分鐘，不需要技術背景。驗收頁本身不用登入；要在哪個網址操作系統、用哪個 Google 帳號登入，Owner 會另外告訴你。

## 怎麼走

1. 打開驗收頁，在「基本資料」填你的稱呼（不用全名，也不用填 Email 或電話）。
2. 照「照順序操作」一步一步在測試環境操作。每一步都寫了**要做什麼／按哪裡／完成後應看到什麼**，對一對畫面就好。
3. 做完一步，按「這一步完成了，下一步」，下一步就會亮起來。
4. 卡住、看不懂或找不到按鈕時，打開那一步的「這一步有問題」，選一個狀態、寫一句話。**全部選填，沒問題就直接下一步。**
5. 走完後回答最後四個問題，一兩句就好。

## 交回回饋

- 按最下方的 **「複製全部回覆」**，看到「已複製全部回覆，現在可以直接貼給 Winnie。」後，直接貼到訊息裡傳給 Winnie。
- 想留一份備份，可以按「下載 Markdown」或「下載 JSON」，檔案只會存在你的電腦。

## 先知道三件事

- **全部資料都是虛構的**：作者林子晴、新書《慢慢來的勇氣》、晨光出版、節目週末書房，聯絡資料都是 `example.invalid`。請照抄，不要改成真人資料。
- **你的回饋只存在這台電腦的瀏覽器，不會上傳到任何地方。** 換電腦、用無痕視窗或清除瀏覽器資料，回饋就會不見，所以記得走完就按「複製全部回覆」。
- **想重來**：按「重新開始」，可以選「重設操作流程、保留回饋」或「操作與回饋全部清除」；不選就不會清掉任何東西。

## 舊版不要再用

`BOOK_INTERVIEW_UI_M0_WIREFRAME.html` 是早期概念稿，頁首會顯示「此版本已過期，請回最新版驗收頁」。它的說明已移到 `archive/COLLEAGUE_UI_REVIEW_GUIDE_M0.md`，只保留作歷史紀錄。
